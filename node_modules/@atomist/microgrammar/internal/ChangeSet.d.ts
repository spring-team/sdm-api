import { PatternMatch } from "../PatternMatch";
export declare class ChangeSet {
    private initialContent;
    private changes;
    constructor(initialContent: string);
    change(match: PatternMatch, to: string): void;
    updated(): string;
}
