import { LangHelper } from "../LangHelper";
export declare class CFamilyLangHelper implements LangHelper {
    /**
     * Strip C/C++ comments
     * @param source
     */
    stripComments(source: string): string;
    stripWhitespace(source: string): string;
    canonicalize(src: string): string;
    private strip(src);
}
