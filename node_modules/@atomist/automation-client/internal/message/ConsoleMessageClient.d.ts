import { Destination, MessageClient, MessageOptions } from "../../spi/message/MessageClient";
import { DefaultSlackMessageClient, MessageClientSupport } from "../../spi/message/MessageClientSupport";
/**
 * Clearly display messages with channels and recipients (if DMs) on the console.
 */
export declare class ConsoleMessageClient extends MessageClientSupport implements MessageClient {
    protected doSend(msg: any, destinations: Destination | Destination[], options?: MessageOptions): Promise<void>;
}
export declare const consoleMessageClient: DefaultSlackMessageClient;
