import { WebSocketRequestProcessor } from "./WebSocketRequestProcessor";
export declare class WebSocketClient {
    private registrationCallback;
    private options;
    private requestProcessor;
    constructor(registrationCallback: () => any, options: WebSocketClientOptions, requestProcessor: WebSocketRequestProcessor);
    start(): Promise<void>;
}
export interface WebSocketClientOptions {
    registrationUrl: string;
    graphUrl: string;
    token: string;
    termination?: {
        gracePeriod?: number;
        graceful?: boolean;
    };
    compress?: boolean;
    timeout?: number;
}
