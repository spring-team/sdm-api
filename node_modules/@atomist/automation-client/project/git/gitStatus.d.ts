export interface GitStatus {
    isClean: boolean;
    ignoredChanges: string[];
    raw: string;
    sha: string;
    branch: string;
    upstream?: {
        branch: string;
        inSync: boolean;
    };
}
export declare function isFullyClean(gs: GitStatus): boolean;
export declare function runStatusIn(baseDir: string): Promise<GitStatus>;
