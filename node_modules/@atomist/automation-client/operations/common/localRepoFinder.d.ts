import { RepoFinder } from "./repoFinder";
/**
 * Look for repos under /org/repo format, from current working directory
 */
export declare function twoTierDirectoryRepoFinder(cwd: string): RepoFinder;
