"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function fromListRepoFinder(projects) {
    if (projects.some(p => !p.id)) {
        throw new Error("Not all projects have id");
    }
    return () => Promise.resolve(projects.map(p => p.id));
}
exports.fromListRepoFinder = fromListRepoFinder;
function fromListRepoLoader(projects) {
    if (projects.some(p => !p.id)) {
        throw new Error("Not all projects have id");
    }
    return (id) => Promise.resolve(projects.find(p => p.id === id));
}
exports.fromListRepoLoader = fromListRepoLoader;
//# sourceMappingURL=fromProjectList.js.map