import { GitProject } from "../../project/git/GitProject";
import { DirectoryManager } from "../../spi/clone/DirectoryManager";
import { ProjectOperationCredentials } from "./ProjectOperationCredentials";
import { RepoLoader } from "./repoLoader";
/**
 * Materialize from github
 * @param credentials provider token
 * @param directoryManager strategy for handling local storage
 * @return function to materialize repos
 */
export declare function gitHubRepoLoader(credentials: ProjectOperationCredentials, directoryManager?: DirectoryManager): RepoLoader<GitProject>;
