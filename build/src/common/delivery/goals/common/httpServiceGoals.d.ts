import { Goals } from "../Goals";
/**
 * Goals for an Http service, mirroring a typical flow through
 * staging to production.
 * Goal sets are normally user defined, so this is largely
 * an illustration.
 * @type {Goals}
 */
export declare const HttpServiceGoals: Goals;
export declare const LocalDeploymentGoals: Goals;
export declare const UndeployEverywhereGoals: Goals;
export declare const RepositoryDeletionGoals: Goals;
