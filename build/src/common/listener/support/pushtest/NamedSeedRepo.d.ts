import { PushTest } from "../../PushTest";
/**
 * Is this a seed repo, based on the naming convention
 * that such repos have "-seed" in their name
 * @param {PushListenerInvocation} pi
 * @constructor
 */
export declare const NamedSeedRepo: PushTest;
